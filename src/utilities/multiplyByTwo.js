/**
 * @param {number} x the number to multiply
 * @returns {number} the multiplied by two value
 * @description multiplyTwo multiplies a given number by two
 */
export const multiplyByTwo = (x) => x * 2;
